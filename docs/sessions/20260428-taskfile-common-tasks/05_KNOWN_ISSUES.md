# Known Issues

- The repository already had a `Taskfile.yml`; this change is a simplification rather than a first-time addition.
- No runtime validation was needed beyond checking that the task definitions map to existing package scripts.

