# Testing Strategy

- Verify the task definitions against `package.json`.
- Confirm the file diff only changes the task surface and session docs.
- For this change, a full repo build/test run is not necessary because no executable code changed.

